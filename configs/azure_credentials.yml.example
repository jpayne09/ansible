# Example Azure credentials configuration file
# Copy this to azure_credentials.yml and fill in your actual values
# NEVER commit the actual credentials file to version control

# Azure Service Principal credentials
subscription_id: "your-subscription-id"
client_id: "your-client-id"
client_secret: "your-client-secret"
tenant_id: "your-tenant-id"

# Resource defaults
default_region: "eastus"
default_resource_group: "rg-ansible-vms"

# Environment-specific settings
environments:
  dev:
    vm_size: "Standard_B2s"
    resource_prefix: "dev"
    location: "eastus"
  
  staging:
    vm_size: "Standard_B2s"
    resource_prefix: "staging"
    location: "eastus"
  
  prod:
    vm_size: "Standard_D2s_v3"
    resource_prefix: "prod"
    location: "eastus"
